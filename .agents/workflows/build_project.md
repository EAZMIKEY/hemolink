---
description: Build the HemoLink project.
---

1. Ensure Node.js >= 20 is installed.
2. Run `npm install` to install all dependencies.
3. (Optional) Run `npm run build` to create a production build.
